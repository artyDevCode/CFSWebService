@model IEnumerable<ModelEntity.CFSActivity>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        <label for="StartDate">Start Date:</label>
        @Html.TextBox("StartDate", null, new { @readonly="readonly" })
        </p>
        <p>
        <label for="EndDate">End Date:</label>
        @Html.TextBox("EndDate", null, new { @readonly = "readonly" })
        </p>
</div>
    <div>
    <div class="col-md-6">
        <h5>Bar Chart</h5>
        @{ Html.RenderAction("Chart", "Chart", new {chartType = "Bar"}); }
        </div>
    <div class="col-md-6">
        <h5>Pie Chart</h5>
        @{ Html.RenderAction("Chart", "Chart", new { chartType = "Pie" }); }
    </div>
        </div>
    <div>
        <div class="col-md-6">
            <h5>Line Chart</h5>
            @{ Html.RenderAction("Chart", "Chart", new { chartType = "Line" }); }
        </div>
        <div class="col-md-6">
            <h5>Pie Chart</h5>
            @{ Html.RenderAction("Chart", "Chart", new { chartType = "other" }); }
        </div>
    </div>
</body>
</html>
